import EaseButtons from "./EaseButtons";

const OnShowInfo = () => {
  return (
    <div className="flex flex-col h-full p-5 font-mono font-semibold text-center place-content-between serious-type md:text-left">
      <h1>Info Place Holder.</h1>
      <EaseButtons />
    </div>
  );
};

export default OnShowInfo;
